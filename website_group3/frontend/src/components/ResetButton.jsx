const ResetButton = ({ backendURL}) => {

    return (
        <td>
            <form>
                <button type='submit'>
                    Reset
                </button>
            </form>
        </td>

    );
};

export default ResetButton;