const DeleteManagerForm = ({ rowObject, backendURL, refreshManagers }) => {

    return (
        <td>
            <form>
                <button type='submit'>
                    Delete
                </button>
            </form>
        </td>

    );
};

export default DeleteManagerForm;