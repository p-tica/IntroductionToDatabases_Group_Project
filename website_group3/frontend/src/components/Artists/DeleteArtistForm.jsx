const DeleteArtistForm = ({ rowObject, backendURL, refreshArtists }) => {

    return (
        <td>
            <form>
                <button type='submit'>
                    Delete
                </button>
            </form>
        </td>

    );
};

export default DeleteArtistForm;