# Database Configuration for MariaDB Container
# DO NOT commit the .env file with real credentials to version control

# MariaDB Root Password (for database administration)
DB_ROOT_PASSWORD=rootpassword

# Application Database Credentials
DB_USER=appuser
DB_PASSWORD=apppassword
DB_NAME=recording_studio

# Optional: Override default ports if needed
# BACKEND_PORT=60000
# FRONTEND_PORT=60001
